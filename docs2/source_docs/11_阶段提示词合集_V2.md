# 11_阶段提示词合集_V2

# AI 知识点学习与音频播报系统 - V2 阶段提示词合集

## 总提示词

```text
请先阅读 docs/README_使用顺序_精简版.md、docs/10_验收清单.md，以及 docs2/README_使用顺序_V2.md。

当前目标是开发 V2。

重要规则：
1. V2 必须建立在 V1 之上。
2. 任何阶段开始前，不能破坏 V1 主流程。
3. 每次只做 docs2/08_开发阶段计划_V2.md 中的一个阶段。
4. 不要提前开发后续阶段。
5. 不要做登录注册、多用户、云同步、Docker、复杂 RAG。
6. 前端不能直接调用 AI 或 TTS 外部接口。
7. API Key 不能写死到代码。
8. 批量 AI / TTS 操作必须有上限。
9. 本地数据继续保存在 data/。
10. 修改后必须说明改了哪些文件、如何启动、如何测试。

请从阶段 19 开始：V1 最终验收和问题修复。
```

---

## 阶段 19：V1 最终验收和问题修复

```text
请开始阶段 19：V1 最终验收和问题修复。

要求：
1. 只按 docs/10_验收清单.md 检查 V1。
2. 不新增 V2 功能。
3. 不重构。
4. 不修改数据库结构。
5. 发现问题只做最小修复。
6. 检查后端启动、前端启动、主流程、AI、TTS、音频播放、设置保存。
7. 检查 git status，确认 .env、data/、app.db、音频文件没有进入待提交列表。

最后输出：
1. 已通过项。
2. 未通过项。
3. 已修复项。
4. 需要我手动测试的项。
```

---

## 阶段 20：首页统计增强

```text
请开始阶段 20：首页统计增强。

要求：
1. 只做阶段 20。
2. 新增 backend/app/api/routes/stats.py。
3. 新增 backend/app/services/stats_service.py。
4. 实现 GET /api/stats/overview。
5. 实现 GET /api/stats/knowledge-bases。
6. 新增 frontend/src/api/stats.ts。
7. 增强 DashboardView，显示今日练题、正确率、待复习、错题、音频等统计。
8. 不破坏 /api/dashboard/stats。
9. 不新增 review_tasks 表。
10. 不修改 V1 其他页面。

完成后告诉我：
1. 改了哪些文件。
2. 如何启动。
3. 如何测试首页统计。
```

---

## 阶段 21：复习计划数据表和后端接口

```text
请开始阶段 21：复习计划数据表和后端接口。

要求：
1. 只做后端和数据库。
2. 新增 ReviewTask 模型。
3. 注册模型，确保后端启动时自动创建 review_tasks 表。
4. 新增 backend/app/api/routes/review.py。
5. 新增 backend/app/services/review_service.py。
6. 实现 GET /api/review/tasks。
7. 实现 POST /api/review/tasks/generate。
8. 实现 GET /api/review/tasks/{task_id}。
9. 实现 POST /api/review/tasks/{task_id}/complete。
10. 实现 POST /api/review/tasks/{task_id}/snooze。
11. 实现 DELETE /api/review/tasks/{task_id}。
12. 复习生成规则：错题优先、high 重要知识点其次、新知识点再次、同一天不重复生成 pending 任务。
13. 默认最多生成 30 个任务。
14. 不做前端页面。
15. 不做复杂 FSRS。
16. 不做闪卡。

完成后告诉我：
1. 改了哪些文件。
2. 如何确认 review_tasks 表已创建。
3. 如何用接口测试生成和完成复习任务。
```

---

## 阶段 22：复习计划前端页面

```text
请开始阶段 22：复习计划前端页面。

要求：
1. 只做阶段 22。
2. 新增 frontend/src/api/review.ts。
3. 新增 frontend/src/views/ReviewView.vue。
4. 增加菜单和路由 /review。
5. 页面展示复习任务列表。
6. 支持生成今日复习。
7. 支持完成任务：again、hard、good、easy。
8. 支持稍后复习。
9. 支持删除任务。
10. 必须有 loading、错误提示、空状态。
11. loading 时禁用按钮。
12. 不做每日复习音频。
13. 不做闪卡。
14. 不做复杂图表。

完成后告诉我：
1. 改了哪些文件。
2. 如何启动。
3. 如何测试复习计划页面。
```

---

## 阶段 23：学习统计接口和页面

```text
请开始阶段 23：学习统计接口和页面。

要求：
1. 只做阶段 23。
2. 完善 /api/stats/overview。
3. 完善 /api/stats/knowledge-bases。
4. 可选实现 /api/stats/trends。
5. 优先从现有表聚合统计。
6. 不新增 study_stats 表。
7. 可增强 DashboardView，也可新增 StatsView。
8. 不引入复杂图表库。
9. 无数据时显示 0。
10. 不修改 V1 主流程。

完成后告诉我：
1. 改了哪些文件。
2. 如何验证统计数字。
3. 哪些统计是精确值，哪些是粗略掌握度。
```

---

## 阶段 24：批量知识点音频

```text
请开始阶段 24：批量知识点音频。

要求：
1. 只做阶段 24。
2. 扩展 audio_service.py 文件命名能力。
3. 扩展 tts_service.py，支持多知识点合集播报文本。
4. 实现 POST /api/tts/generate-batch。
5. 支持 audio_type=collection。
6. 校验 TTS_BATCH_MAX_POINTS。
7. 校验 TTS_BATCH_MAX_CHARS。
8. KnowledgePointListView 增加表格多选。
9. KnowledgePointListView 增加批量生成音频按钮。
10. AudioView 增加 audio_type 筛选。
11. 生成成功后能播放。
12. 不做每日复习音频。
13. 不做错题音频。
14. 不做音频合并。
15. 不做后台任务队列。
16. V1 单个知识点音频必须仍可用。

完成后告诉我：
1. 改了哪些文件。
2. 如何测试批量知识点音频。
3. 如何确认 V1 单个知识点音频没有坏。
```

---

## 阶段 25：每日复习音频和错题音频

```text
请开始阶段 25：每日复习音频和错题音频。

要求：
1. 只做阶段 25。
2. 基于阶段 24 的音频能力扩展。
3. 实现 POST /api/tts/generate-daily-review。
4. 实现 POST /api/tts/generate-wrong-questions。
5. 支持 audio_type=daily_review。
6. 支持 audio_type=wrong_question。
7. ReviewView 增加生成每日复习音频按钮。
8. WrongQuestionView 支持多选错题。
9. WrongQuestionView 增加生成错题音频按钮。
10. AudioView 能筛选每日复习音频和错题音频。
11. 今日无复习任务时返回明确提示。
12. 不做播放队列。
13. 不做音频合并。

完成后告诉我：
1. 改了哪些文件。
2. 如何测试每日复习音频。
3. 如何测试错题复习音频。
```

---

## 阶段 26：AI 长文本分段

```text
请开始阶段 26：AI 长文本分段。

要求：
1. 只做阶段 26。
2. 新增 backend/app/services/text_splitter.py。
3. /api/materials/import-and-extract 支持 enable_split。
4. 短文本仍按 V1 流程提取。
5. 长文本按 AI_SEGMENT_SIZE 自动分段。
6. 分段数不能超过 AI_MAX_SEGMENTS。
7. 逐段调用 AI 提取知识点。
8. 合并结果。
9. 返回 split_used、segment_count、created_count、skipped_count。
10. MaterialImportView 展示分段数量和新增/跳过数量。
11. loading 期间禁用按钮。
12. 不做 JSON 修复。
13. 不做闪卡。

完成后告诉我：
1. 改了哪些文件。
2. 如何测试短文本不变。
3. 如何测试长文本分段。
4. 如何测试超过分段上限。
```

---

## 阶段 27：AI 去重和 JSON 修复

```text
请开始阶段 27：AI 去重和 JSON 修复。

要求：
1. 只做阶段 27。
2. 新增 backend/app/services/dedup_service.py。
3. 知识点 title 标准化去重。
4. 题目 stem 标准化去重。
5. json_parser 支持 JSON 修复流程。
6. prompt_templates 增加 JSON 修复 Prompt。
7. JSON 修复最多 1 次。
8. 修复失败时返回明确错误。
9. 前端展示 created_count / skipped_count。
10. 不做闪卡。
11. 不做 AI 日志表。
12. 不做 embedding 相似度。
13. 不做向量去重。

完成后告诉我：
1. 改了哪些文件。
2. 如何测试知识点去重。
3. 如何测试题目去重。
4. 如何测试 JSON 修复失败。
```

---

## 阶段 28：知识点闪卡

```text
请开始阶段 28：知识点闪卡。

要求：
1. 只做阶段 28。
2. 新增 Flashcard 模型。
3. 自动创建 flashcards 表。
4. 新增 flashcards 路由。
5. 实现闪卡 CRUD。
6. 实现 POST /api/flashcards/generate-from-point/{knowledge_point_id}。
7. AI 支持生成 basic、reverse、cloze 类型闪卡。
8. KnowledgePointDetailView 增加闪卡区。
9. KnowledgePointDetailView 增加生成闪卡弹窗。
10. 重复 front 跳过。
11. 不做 Anki 导出。
12. 不做闪卡复习算法。
13. 不做独立 FlashcardView，除非实现详情页后仍很简单。

完成后告诉我：
1. 改了哪些文件。
2. 如何确认 flashcards 表已创建。
3. 如何测试生成、编辑、删除闪卡。
```

---

## 阶段 29：导入导出

```text
请开始阶段 29：导入导出。

要求：
1. 只做阶段 29。
2. 新增 backend/app/api/routes/export.py。
3. 实现 GET /api/export/knowledge-points.csv。
4. 实现 GET /api/export/questions.csv。
5. 实现 GET /api/export/wrong-questions.csv。
6. 使用 StreamingResponse。
7. 不落盘生成临时文件。
8. 新增 frontend/src/api/export.ts。
9. 知识点列表增加导出按钮。
10. 错题本增加导出按钮。
11. 空数据时返回明确提示。
12. CSV 中文不能乱码。
13. 不做 apkg。
14. 不做复杂导入。
15. 不做异步导出任务。

完成后告诉我：
1. 改了哪些文件。
2. 如何测试知识点 CSV。
3. 如何测试题目 CSV。
4. 如何测试错题 CSV。
```

---

## 阶段 30：V2 整体联调和最终验收

```text
请开始阶段 30：V2 整体联调和最终验收。

要求：
1. 不新增功能。
2. 按 docs2/10_验收清单_V2.md 检查。
3. 先跑 V1 主流程，确认不回归。
4. 再跑 V2 复习流程。
5. 再跑 V2 批量音频流程。
6. 再跑 V2 AI 分段和去重流程。
7. 再跑 V2 闪卡和导出流程。
8. 检查设置页。
9. 检查 git status。
10. 发现问题只做最小修复。
11. 不重构。

最后输出：
1. 已通过项。
2. 未通过项。
3. 已修复项。
4. 需要我手动测试的项。
5. 剩余风险。
```

---

## 快速版本：只做最小可用 V2

```text
请按最小可用 V2 开发。

只做这些阶段：
1. 阶段 19：V1 最终验收和问题修复
2. 阶段 20：首页统计增强
3. 阶段 21：复习计划数据表和后端接口
4. 阶段 22：复习计划前端页面
5. 阶段 24：批量知识点音频
6. 阶段 30：V2 整体联调和最终验收

暂不做：
1. 学习统计独立页面
2. 每日复习音频
3. 错题音频
4. AI 长文本分段
5. AI JSON 修复
6. 闪卡
7. 导出

每次只做一个阶段。
每个阶段完成后必须说明如何测试。
```

---

## 严格验收提示词

```text
请按 docs2/10_验收清单_V2.md 做验收。

要求：
1. 不新增功能。
2. 不重构。
3. 能自动检查的就运行命令或调用接口。
4. 需要人工操作的给出清晰步骤。
5. 发现问题只做最小修复。
6. 最后输出已通过项、未通过项、已修复项、需要我手动测试的项。
```
