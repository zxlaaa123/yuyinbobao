# 10_验收清单_V3

# AI Study Cast - V3 验收清单

## 1. 验收原则

V3 验收顺序：

```text
1. V1 主流程
2. V2 功能
3. V3 新功能
```

任何 V1/V2 回归都优先修复。

---

## 2. 环境检查

检查：

```text
backend/.venv 存在
frontend/node_modules 存在
data/ 存在
.env 不提交
data/ 不提交
```

命令：

```text
git status
```

通过标准：

```text
.env
data/
app.db
node_modules/
.venv/
dist/
__pycache__/
不进入提交
```

---

## 3. 启动检查

后端：

```text
cd backend
.venv\Scripts\activate
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

前端：

```text
cd frontend
npm run dev
```

健康检查：

```http
GET http://localhost:8000/api/health
```

---

## 4. 构建检查

前端：

```text
cd frontend
npm run build
```

后端：

```text
cd backend
.venv\Scripts\python.exe -m compileall app
```

---

## 5. V1/V2 回归检查

必须跑通：

```text
1. 新建知识库
2. 导入资料
3. 保存并提取知识点
4. 查看知识点
5. 生成题目
6. 刷题
7. 答错进入错题本
8. 生成音频
9. 播放音频
10. 生成复习任务
11. 生成闪卡
12. CSV 导出
13. 设置保存
```

---

## 6. V3 功能验收

## 6.1 前端结构

通过标准：

```text
router/index.ts 存在
layouts/MainLayout.vue 存在
main.ts 简洁
App.vue 不再承担复杂布局
npm run build 通过
```

## 6.2 错误处理

检查：

```text
资料标题为空
AI Key 未配置
TTS Key 未配置
后端关闭
删除失败
```

通过标准：

```text
提示清楚，中文，不出现 undefined。
```

## 6.3 备份恢复

检查：

```text
创建备份
查看备份列表
恢复备份
删除备份
```

通过标准：

```text
备份文件存在
恢复前有确认
恢复后数据可用
```

## 6.4 学习会话

检查：

```text
开始练习
完成答题
查看学习记录
```

通过标准：

```text
记录题目数
记录正确数
记录正确率
```

## 6.5 AI 日志

检查：

```text
成功提取知识点
故意使用错误模型或错误 key
查看日志
```

通过标准：

```text
成功和失败均有记录
不显示 API Key
错误信息可读
```

## 6.6 搜索

检查：

```text
搜索知识点标题
搜索资料标题
搜索不存在关键词
```

通过标准：

```text
结果能跳转
无结果有空状态
```

---

## 7. 最终通过标准

```text
V1 主流程不回归
V2 功能可用
V3 新功能可用
前端构建通过
后端编译通过
git status 清楚
无敏感信息提交
```

